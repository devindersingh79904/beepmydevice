# --- Web framework -----------------------------------------------------------
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0

# --- Database ----------------------------------------------------------------
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.0

# --- Validation & settings ---------------------------------------------------
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# --- Authentication ----------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.1

# --- Push notifications ------------------------------------------------------
firebase-admin==6.2.0
# APNs is called over plain HTTP/2 rather than through a client library.
# apns2 pins PyJWT<2.0 while firebase-admin requires PyJWT>=2.5, so the two
# cannot coexist; Apple's token-authenticated API is a signed JWT and one POST,
# which python-jose and httpx already cover.
httpx[http2]==0.25.2

# --- Configuration -----------------------------------------------------------
python-dotenv==1.0.0

# --- Testing -----------------------------------------------------------------
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# --- Tooling -----------------------------------------------------------------
black==23.11.0
pylint==3.0.2
mypy==1.7.1
